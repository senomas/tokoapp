{
  "name": "tokoapp",
  "version": "0.0.1",
  "scripts": {
    "dev": "svelte-kit dev",
    "build": "svelte-kit build",
    "docker-build": "node util/clean-package.js && cd ../supabase && docker-compose build web e2e-test",
    "preview": "npm run docker-build && npm run predev && cd ../supabase && docker-compose up -d web nginx",
    "package": "svelte-kit package",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-check --tsconfig ./tsconfig.json --watch",
    "lint": "prettier --ignore-path .gitignore --check --plugin-search-dir=. . && eslint --ignore-path .gitignore .",
    "format": "prettier --ignore-path .gitignore --write --plugin-search-dir=. .",
    "supabase": "cd ../supabase && docker-compose up -d web nginx studio kong auth rest realtime storage meta db",
    "supabase-stop": "cd ../supabase && docker-compose down -v",
    "flyway": "docker run --rm --network=tokoapp_default -v `pwd`/../sql:/flyway/sql -v `pwd`/../flyway.conf:/flyway/conf/flyway.conf flyway/flyway:8.5.2",
    "init-data": "npm run flyway -- -target=2 migrate && node init-user.js && npm run flyway migrate",
    "predev": "npm run supabase-stop && npm run supabase && npm run init-data && npm run flyway migrate",
    "postdev": "npm run supabase-stop",
    "test": "cd ../supabase && docker-compose up e2e-test",
    "pretest": "(mkdir screenshot || rm -rf screenshot/*) && (mkdir videos || rm -rf videos/*) && npm run docker-build && npm run predev && cd ../supabase && docker-compose up -d web nginx",
    "qtest": "cd ../supabase && docker-compose up e2e-test",
    "preqtest": "(mkdir screenshot || rm -rf screenshot/*) && (mkdir videos || rm -rf videos/*) && npm run docker-build && cd ../supabase && docker-compose up -d web nginx"
  },
  "devDependencies": {
    "@playwright/test": "^1.20.0",
    "@sveltejs/adapter-auto": "next",
    "@sveltejs/adapter-node": "^1.0.0-next.70",
    "@sveltejs/adapter-static": "next",
    "@sveltejs/kit": "next",
    "@typescript-eslint/eslint-plugin": "^5.10.1",
    "@typescript-eslint/parser": "^5.10.1",
    "autoprefixer": "^10.4.2",
    "eslint": "^7.32.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-svelte3": "^3.2.1",
    "postcss": "^8.4.7",
    "prettier": "^2.5.1",
    "prettier-plugin-svelte": "^2.5.0",
    "svelte": "^3.44.0",
    "svelte-check": "^2.2.6",
    "svelte-preprocess": "^4.10.1",
    "tailwindcss": "^3.0.23",
    "tslib": "^2.3.1",
    "typescript": "~4.5.4"
  },
  "type": "module",
  "dependencies": {
    "@supabase/supabase-js": "^1.30.7",
    "js-yaml": "^4.1.0"
  }
}